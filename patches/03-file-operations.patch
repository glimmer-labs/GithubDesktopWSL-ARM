diff --git a/app/src/lib/git/cherry-pick.ts b/app/src/lib/git/cherry-pick.ts
index 7e9f184..91d4c0f 100644
--- a/app/src/lib/git/cherry-pick.ts
+++ b/app/src/lib/git/cherry-pick.ts
@@ -24,8 +24,7 @@ import { stageManualConflictResolution } from './stage'
 import { getCommit } from '.'
 import { IMultiCommitOperationProgress } from '../../models/progress'
 import { join } from 'path'
-import { readFile } from 'fs/promises'
-import { pathExists } from '../path-exists'
+import { wslReadFile as readFile, wslPathExists as pathExists } from '../wsl'
 
 /** The app-specific results from attempting to cherry pick commits*/
 export enum CherryPickResult {
@@ -162,17 +161,19 @@ export async function cherryPick(
     )
   }
 
-  // --empty=keep follows pattern of making sure someone cherry
-  //  picked commit summaries appear in target branch history even tho they may
-  //  be empty. This flag also results in the ability to cherry pick empty
-  //  commits (thus, --allow-empty is not required.)
+  // --keep-redundant-commits keeps cherry-picked commit summaries in the
+  //  target branch history even when they become empty. Upstream uses the
+  //  newer `--empty=keep` spelling, but the WSL daemon runs the distro's git,
+  //  which may predate git 2.45 where `--empty=<...>` was added to cherry-pick.
+  //  `--keep-redundant-commits` is the long-standing equivalent and is accepted
+  //  by both old and new git. See issue #56.
   //
   // -m 1 makes it so a merge commit always takes the first parent's history
   //  (the branch you are cherry-picking from) for the commit. It also means
   //  there could be multiple empty commits. I.E. If user does a range that
   //  includes commits from that merge.
   const result = await git(
-    ['cherry-pick', ...commits.map(c => c.sha), '--empty=keep', '-m 1'],
+    ['cherry-pick', ...commits.map(c => c.sha), '--keep-redundant-commits', '-m 1'],
     repository.path,
     'cherry-pick',
     baseOptions
@@ -216,7 +217,7 @@ function parseCherryPickResult(result: IGitResult): CherryPickResult {
 export async function getCherryPickSnapshot(
   repository: Repository
 ): Promise<ICherryPickSnapshot | null> {
-  if (!isCherryPickHeadFound(repository)) {
+  if (!(await isCherryPickHeadFound(repository))) {
     // If there no cherry pick head, there is no cherry pick in progress.
     return null
   }
@@ -298,7 +299,7 @@ export async function getCherryPickSnapshot(
   } catch {
     // could not parse sequencer files
 
-    if (!isCherryPickHeadFound(repository)) {
+    if (!(await isCherryPickHeadFound(repository))) {
       // We redo this check just because a user technically could end the
       // cherry-pick by the time we got here.
       return null
@@ -411,7 +412,7 @@ export async function continueCherryPick(
   }
 
   // make sure cherry pick is still in progress to continue
-  if (await !isCherryPickHeadFound(repository)) {
+  if (!(await isCherryPickHeadFound(repository))) {
     return CherryPickResult.UnableToStart
   }
 
diff --git a/app/src/lib/git/description.ts b/app/src/lib/git/description.ts
index ca15d74..6da6582 100644
--- a/app/src/lib/git/description.ts
+++ b/app/src/lib/git/description.ts
@@ -1,5 +1,8 @@
 import * as Path from 'path'
-import { readFile, writeFile } from 'fs/promises'
+import {
+  wslReadFile as readFile,
+  wslWriteFile as writeFile,
+} from '../wsl'
 
 const GitDescriptionPath = '.git/description'
 
diff --git a/app/src/lib/git/diff.ts b/app/src/lib/git/diff.ts
index b6797fc..3c86d47 100644
--- a/app/src/lib/git/diff.ts
+++ b/app/src/lib/git/diff.ts
@@ -24,6 +24,7 @@ import {
 import { DiffParser } from '../diff-parser'
 import { getOldPathOrDefault } from '../get-old-path'
 import { readFile, writeFile, unlink } from 'fs/promises'
+import { isWSLPath, wslReadFile } from '../wsl'
 import { getTempFilePath } from '../file-system'
 import { forceUnwrap } from '../fatal-error'
 import { git } from './core'
@@ -894,7 +895,10 @@ export async function getWorkingDirectoryImage(
   repository: Repository,
   file: FileChange
 ): Promise<Image> {
-  const contents = await readFile(Path.join(repository.path, file.path))
+  const filePath = Path.join(repository.path, file.path)
+  const contents = isWSLPath(filePath)
+    ? (await wslReadFile(filePath)) as Buffer
+    : await readFile(filePath)
   return new Image(
     contents.buffer,
     contents.toString('base64'),
diff --git a/app/src/lib/git/gitignore.ts b/app/src/lib/git/gitignore.ts
index 671f703..a3ffe5f 100644
--- a/app/src/lib/git/gitignore.ts
+++ b/app/src/lib/git/gitignore.ts
@@ -2,7 +2,12 @@ import * as Path from 'path'
 import * as FS from 'fs'
 import { Repository } from '../../models/repository'
 import { getConfigValue } from './config'
-import { writeFile } from 'fs/promises'
+import {
+  isWSLPath,
+  wslReadFile,
+  wslWriteFile as writeFile,
+  wslUnlink,
+} from '../wsl'
 
 /**
  * Read the contents of the repository .gitignore.
@@ -16,6 +21,15 @@ export async function readGitIgnoreAtRoot(
 ): Promise<string | null> {
   const ignorePath = Path.join(repository.path, '.gitignore')
 
+  if (isWSLPath(ignorePath)) {
+    try {
+      const data = await wslReadFile(ignorePath, 'utf8')
+      return data
+    } catch {
+      return null
+    }
+  }
+
   return new Promise<string | null>((resolve, reject) => {
     FS.readFile(ignorePath, 'utf8', (err, data) => {
       if (err) {
@@ -44,6 +58,9 @@ export async function saveGitIgnore(
   const ignorePath = Path.join(repository.path, '.gitignore')
 
   if (text === '') {
+    if (isWSLPath(ignorePath)) {
+      return wslUnlink(ignorePath)
+    }
     return new Promise<void>((resolve, reject) => {
       FS.unlink(ignorePath, err => {
         if (err) {
diff --git a/app/src/lib/git/merge.ts b/app/src/lib/git/merge.ts
index db52ba8..cecf576 100644
--- a/app/src/lib/git/merge.ts
+++ b/app/src/lib/git/merge.ts
@@ -2,7 +2,7 @@ import { join } from 'path'
 import { git, HookCallbackOptions } from './core'
 import { GitError } from 'dugite'
 import { Repository } from '../../models/repository'
-import { pathExists } from '../path-exists'
+import { wslPathExists as pathExists } from '../wsl'
 import { createMultiOperationTerminalOutputCallback } from './multi-operation-terminal-output'
 
 export enum MergeResult {
diff --git a/app/src/lib/git/rebase.ts b/app/src/lib/git/rebase.ts
index 17677ae..1e01128 100644
--- a/app/src/lib/git/rebase.ts
+++ b/app/src/lib/git/rebase.ts
@@ -29,8 +29,7 @@ import { getStatus } from './status'
 import { getCommitsBetweenCommits } from './rev-list'
 import { Branch } from '../../models/branch'
 import { join } from 'path'
-import { readFile } from 'fs/promises'
-import { pathExists } from '../path-exists'
+import { wslReadFile as readFile, wslPathExists as pathExists } from '../wsl'
 
 /** The app-specific results from attempting to rebase a repository */
 export enum RebaseResult {
diff --git a/app/src/lib/git/submodule.ts b/app/src/lib/git/submodule.ts
index 266b20b..62177c1 100644
--- a/app/src/lib/git/submodule.ts
+++ b/app/src/lib/git/submodule.ts
@@ -1,7 +1,7 @@
 import { git, IGitStringExecutionOptions } from './core'
 import { Repository } from '../../models/repository'
 import { SubmoduleEntry } from '../../models/submodule'
-import { pathExists } from '../path-exists'
+import { wslPathExists as pathExists } from '../wsl'
 import { executionOptionsWithProgress, IGitOutput } from '../progress'
 import {
   envForRemoteOperation,
diff --git a/app/src/lib/stores/app-store.ts b/app/src/lib/stores/app-store.ts
index 6a317df..c3415b2 100644
--- a/app/src/lib/stores/app-store.ts
+++ b/app/src/lib/stores/app-store.ts
@@ -368,7 +368,7 @@ import {
   getNotificationsEnabled,
 } from './notifications-store'
 import * as ipcRenderer from '../ipc-renderer'
-import { pathExists } from '../path-exists'
+import { wslPathExists as pathExists } from '../wsl'
 import { offsetFromNow } from '../offset-from'
 import { findContributionTargetDefaultBranch } from '../branch'
 import { ValidNotificationPullRequestReview } from '../valid-notification-pull-request-review'
@@ -9006,7 +9006,7 @@ export class AppStore extends TypedBaseStore<IAppState> {
     repository: Repository,
     sourceBranch: Branch | null
   ): Promise<void> {
-    if (!isCherryPickHeadFound(repository)) {
+    if (!(await isCherryPickHeadFound(repository))) {
       return
     }
 
